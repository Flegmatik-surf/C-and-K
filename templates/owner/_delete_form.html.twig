<form method="post" action="{{ path('owner_delete', {'id': owner.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer ce propriétaire?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ owner.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
