<form method="post" action="{{ path('region_delete', {'id': region.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette région?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ region.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
